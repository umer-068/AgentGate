apiVersion: v1
kind: Service
metadata:
  name: {{ include "agentgate.name" . }}
  labels:
    {{- include "agentgate.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      name: http
  selector:
    {{- include "agentgate.labels" . | nindent 4 }}
